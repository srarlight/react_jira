{
  "routes": [
    {
      "method": "GET",
      "path": "/details",
      "handler": "detail.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/details/count",
      "handler": "detail.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/details/:id",
      "handler": "detail.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/details",
      "handler": "detail.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/details/:id",
      "handler": "detail.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/details/:id",
      "handler": "detail.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
